package pages.mobile;

import driver.MobileDriver;
import org.openqa.selenium.WebDriver;

public class FirstPage extends MobileDriver {

    public FirstPage(WebDriver driver) {
        super(driver);
    }
}
